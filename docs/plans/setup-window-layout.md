# setup-window-layout

- 대상: `app` SwiftPM 패키지와 설정 창 테스트
- 시작 커밋: `a13fa48`
- 기준 트리: `/Users/choongjaelee/Codes/terminal-checkout/.claude/worktrees/setup-window-layout-review` (`worktree-setup-window-layout-review`, `a13fa48`) · 작업 트리: `/Users/choongjaelee/Codes/terminal-checkout-setup-window-layout-work` (`setup-window-layout-work`, `a13fa48`)
- 현재: R7 수정 중 · 마지막 승격 7b39ea9 · 리뷰 중 없음 · 게이트 미실행(드라이버 환경에서 실행 예정)
- 최근 검증자 판정: "이 계획으로 시작하는 데 합의한다. 단 R1 첫 배정은 항목 1∼3의 재현·계측뿐이고, 항목 4의 수정 배정은 red를 본 뒤에만 나간다." · 원문 `/private/tmp/claude-501/-Users-choongjaelee-Codes-terminal-checkout/4ffc53dd-bc2a-45a7-9448-a47a2106d5f6/scratchpad/R0-review1.md`

## 배경 — 확인한 원천

- [이슈 #34](https://github.com/dazebug/terminal-checkout/issues/34) — 권한 부여 또는 재설치 후 재부여 때 설정 창의 빈 띠와 상단 클리핑이 발생하며, 사용자 관측상 권한 전이가 트리거다.
- [PR #36](https://github.com/dazebug/terminal-checkout/pull/36) — headless 가용 높이 681pt에서 콘텐츠 721.5pt, iTerm2 선택 시 830.5pt인 수치와 스크롤 인수 분기를 제시한다.
- [8fccab5](https://github.com/dazebug/terminal-checkout/commit/8fccab5) — 창 크기 계산을 `FittedContentStackView.layout()`으로 옮기고, 가시성 토글 뒤 정착·측정과 visible-frame clamp를 한 곳에 둔 선행 수정이다.
- [9460d5a](https://github.com/dazebug/terminal-checkout/commit/9460d5a6fb7cca66fe799b35b967f179a67ca069) — 이슈 본문이 지목한 빌드이며, 설정 창의 접근성 안내 문구와 상태 길이가 현재 레이아웃 입력에 포함된다.
- `CLAUDE.md` — 설정 창은 문서 스택이 측정의 단일 소유자이고, 비뒤집힌 documentView의 좌표·권한 상태·레이아웃 순서를 보존해야 한다는 프로젝트 규약이다.
- `docs/context/testing.md` — `visibleFrameOverride` setter가 소유 레이아웃을 dirty하게 해야 하며, 테스트가 한 방향 oracle만 가지면 환경에 따라 계속 초록일 수 있다는 D322·D323의 근거다.
- `docs/context/signing-and-permissions.md` — ad-hoc 재서명이 Accessibility TCC 상태를 무효화할 수 있어 설치 중 재부여 경로도 기기 검증 범위에 들어간다.

## 목표

Accessibility가 false에서 true로 바뀌는 동안 설정 창이 열려 있어도, `refresh()`가 숨긴 콘텐츠와 창·스크롤 document의 크기가 같은 정착된 레이아웃을 사용한다.
콘텐츠가 화면에 들어오면 창 높이뿐 아니라 document와 viewport의 배치도 일치하고, 들어오지 않으면 창만 visible frame으로 clamp되며 document는 줄어들거나 겹치지 않는다.
레이아웃을 고정점까지 반복한 뒤 한 번 더 돌려도 창 크기·document 배치·스크롤 위치가 변하지 않으며, 권한 전이·재설치 후 재부여·터미널 선택·도구·소켓·언어·상태 라벨 변화를 같은 불변식으로 검증한다.

## 완료의 정의

- 반드시 재현해 막아야 끝인 실패: Accessibility를 허용하지 않은 Warp 설정 창을 정착시킨 뒤 권한을 허용하고 System Settings에서 돌아와 key가 되게 하는 입력, 그리고 창이 열린 상태에서 `./install.sh` 후 같은 재부여를 하는 입력 → 창 높이는 이전의 큰 값에 남고 짧아진 비뒤집힌 document가 아래에 놓이는 빈 띠 또는 새 콘텐츠의 상단 클리핑.
- acceptance oracle: 드라이버 환경에서 먼저 최소 provider seam만 연결한 현재 트리에 반복 settle·계측을 적용해 red가 되는지 확인한다. red가 없으면 수정으로 진행하지 않고 그 사실과 실기기와 다른 경계를 보고한다. red가 확인된 뒤에만 green을 요구하며, 콘텐츠가 화면에 들어오는 경우 `contentHeight(window) == rootStack.fittingSize.height`와 `rootStack.frame.height == rootStack.fittingSize.height`가 각각 0.5pt 이내이고, `scroll.documentVisibleRect.maxY == document.frame.maxY` 및 `card.header`가 viewport 안에 있어야 한다. 짧은 document에서는 document frame과 documentVisibleRect의 양 끝이 일치해 viewport에 빈 영역이 없어야 한다. 콘텐츠가 화면에 들어오지 않는 경우 `contentHeight(window) == visible.height`가 1pt 이내이고 document는 clip보다 커야 한다. settle을 고정점까지 반복한 뒤 한 번 더 실행해 창 크기·document frame·documentVisibleRect·scroll origin이 tolerance 안에서 바뀌지 않아야 하며, 언어 변경의 scroll anchor·focus·selection과 모든 status label의 wrap 계약도 함께 green이어야 한다.
- 기기 acceptance 1 (R1 재현 red 직후 한 번 실행): `tccutil reset Accessibility com.dazebug.terminal-checkout` → 앱 실행 → 설정 창이 정상인지 확인 → [Request Accessibility Permission] → System Settings에서 Terminal Checkout 허용 → 설정 창으로 복귀 → 빈 띠·상단 클리핑 없이 계속 정상인지 확인한다.
- 기기 acceptance 2: 창이 열린 설치 위에 `./install.sh` 실행 → ad-hoc 재서명으로 권한이 reset된 것을 확인 → Warp claude 입력용 Accessibility를 다시 허용 → 설정 창으로 복귀 → 같은 레이아웃 불변식이 계속 성립하는지 확인한다. 두 결과는 종결 시 PR 본문으로 승계한다.
- 코퍼스 범위: `SetupWindowController.swift`의 직접 `isHidden` 대입을 세는 `rg -n "\\.isHidden\\s*=" app/Sources/App/SetupWindowController.swift | wc -l` → 18, raw `refresh()` match를 세는 `rg -n "refresh\\(\\)" app/Sources/App/SetupWindowController.swift | wc -l` → 23(주석·정의 포함)을 근거로 삼고, 실제 값의 출처는 전수 소탕 표로 열거한다. 여기에 `supportedLocales`가 제공하는 5개 앱 언어와 `SetupWindowLayoutTests.swift:83∼115`의 각 측정 경로를 포함한다.
- 원자성·부분 실패·롤백 경계: N/A — UI 상태를 다시 읽어 같은 결과를 만드는 레이아웃이며 외부 명령을 이 항목에서 실행하지 않는다. `window.setContentSize`와 scroll restore는 반복 가능한 상태 갱신이어야 하고, 테스트용 Accessibility provider는 각 테스트의 `defer` 또는 tearDown에서 실제 provider로 복구한다. 실제 TCC 변경은 기기 acceptance에서만 수행하며 실패 시 코드 green으로 간주하지 않는다.

## 비목표 — 건드리지 않는다

- `app/Sources/Core/WarpControl.swift`의 실제 TCC API와 Warp 입력 전달 — 설정 창이 읽는 상태의 seam만 두고 권한 의미나 입력 게이트는 바꾸지 않는다.
- `install.sh`의 cdhash 비교·TCC reset과 터미널 실행·Chrome relay·socket protocol — 재설치 경로는 재현 입력과 수동 acceptance로만 다룬다.
- `capturePlace`·`restore`의 언어 변경 계약을 숫자 offset이나 flipped document 가정으로 단순화하는 변경 — anchor role, UTF-16 selection, 비뒤집힌 `maxY` 계산을 유지한다.
- 레이아웃 문제와 무관한 카탈로그 문구·옵션 저장·확장 기능 — 상태 라벨 길이를 재현하는 데 필요한 기존 리소스 외에는 번역을 바꾸지 않는다.

## 불변 원칙

가시성 토글 또는 intrinsic-size 변경 → constraint/layout pass 정착 → `fittingSize` 측정 → 창 크기·visible-frame 보정의 순서를 `FittedContentStackView`의 단일 경로로 유지한다.
`layout()`은 `fittingSize`와 clamp target을 측정하는 유일한 지점으로 유지하고, `setContentSize`와 `moveInside`의 창 프레임 적용은 그 pass가 반환한 뒤 다음 main-queue turn으로 미룬다. pending update는 하나만 두고 실행 시 최신 `lastRequestedSize`를 적용하며, 창이 닫히면 lifecycle guard가 적용을 버린다. 두 설정 창 테스트는 `SetupWindowTestSupport.settle` 하나만 사용하고, 그 helper는 `layoutSubtreeIfNeeded()`로 결함을 고치지 않고 main run loop만 펌프해 창 크기·fittingSize·document frame·documentVisibleRect·scroll origin snapshot이 변하지 않고 pending window update와 restore completion도 없을 때까지 반복하되 이름 있는 상한 N회에 도달하면 실패한다.
각 pass의 `layout()` 호출 수와 `fittingSize`, clamp target, window content height, document frame, documentVisibleRect, scroll origin을 test-only 계측으로 기록한다. 계측은 원인 확인 뒤 제거하거나 테스트 계약으로 남길지를 결정하며, 계측 없이 수정 방향을 고르지 않는다.
`visibleFrameOverride`는 측정뿐 아니라 이 layout cycle의 visible rect를 대리한다. setter는 첫 layout 이후 대입되어도 소유 스택에 layout을 예약해야 하며, 테스트 호출자가 별도의 `needsLayout`을 기억해야 하는 계약으로 되돌리지 않는다.
`lastRequestedSize`의 early return은 현재 document와 새 `fittingSize`가 반영된 target을 버리는 근거가 될 수 없다. 콘텐츠가 화면에 들어올 때 창이 콘텐츠보다 큰 상태를 허용하지 않고, 화면보다 큰 콘텐츠는 stack을 squeeze하지 않은 채 scroll view가 인수한다.
비뒤집힌 documentView에서 viewport 상단은 `documentVisibleRect.maxY`로 계산하고, 짧은 document가 clip 아래에 놓이거나 긴 document의 첫 카드가 원점에서 잘리는 증상을 같은 창·document 높이 불변식으로 막는다.
`.fullSizeContentView` 설정 창은 `FittedContentStackView.edgeInsets.top == 38`로 타이틀바를 비키므로 `buildContent()`의 `NSScrollView.automaticallyAdjustsContentInsets`를 false로 유지한다. AppKit의 자동 title-bar inset과 stack의 자체 inset을 동시에 적용하지 않는다.
높이 equality와 배치 equality는 별도 oracle이다. document가 clip보다 짧을 때의 빈 위쪽 영역, document가 clip보다 길 때 top position에서 header가 잘리는 경우를 각각 독립적으로 실패시킨다.
새 창의 화면은 생성 직후 `NSScreen.main`을 한 번 읽어 placeholder를 그 visible rect 안에 놓는 launch-time 정책으로 정한다. 그 뒤 `layout()`은 `window.screen` 또는 `visibleFrameOverride`에서 한 번 만든 visible rect를 `lastVisibleFrame`에 보존하고, 첫 실제 target의 `centerInside`와 `moveInside`에 같은 rect를 전달한다. `NSWindow.center()`로 화면을 다시 선택하지 않는다. `window.screen`이 nil이면 아직 물리적 display가 정해지지 않은 상태이므로 `NSScreen.main`을 다시 읽어 다른 화면을 고르지 않고 clamp·center를 생략한다. 사용자의 디스플레이 구성은 Mac Pro + 외장 2대(가로 1, 세로 1)로 확인됐으며, 두 화면의 선택 불일치는 기기 acceptance로만 검증한다. 단위 테스트는 순수 `centerInside`·`moveInside` 산술 계약만 고정한다. `windowWillClose`는 pending update를 중단하고 `windowDidBecomeKey`가 재활성화 때 새 측정을 허용한다.
`refresh()`는 상태 라벨·카드와 섹션의 현재 값을 다시 쓰되 ordinary refresh에서 view tree를 재생성하지 않는다. `rebuildForLanguageChange()`는 `capturePlace` → build → refresh → layout 정착 → `restore` 순서와 scroll anchor·포커스·UTF-16 selection 복원을 유지한다.
`rebuildForLanguageChange()`의 `restore(place:)`는 새 stack의 deferred window-size application과 clip layout completion 뒤에 실행한다. 이전 clip 크기에 anchor·focus·selection을 복원하지 않는다.
상태 라벨은 `makeStatusLabel(font:)`의 비단일행·wrap·`maximumNumberOfLines == 0`·`preferredMaxLayoutWidth == setupTextWidth` 계약을 유지한다. 텍스트 길이만 바뀌는 권한 상태와 tool/socket 결과도 같은 레이아웃 oracle의 대상이다.
테스트는 live `accessibilityIsTrusted()`나 실제 System Settings 권한을 뒤집지 않는다. 앱 계층에만 주입 가능한 provider를 만들고, 테스트가 끝나면 실제 동작으로 되돌린다. provider가 만든 red는 실제 TCC UI를 대신하는 것이 아니라 `refresh()`의 상태 전이와 레이아웃 경계를 검증한다. R1의 provider seam과 반복 settle/계측은 재현을 위한 최소 변경이며, red 확인 전에는 production layout fix를 배정하지 않는다.

## 배치 점검 (0라운드)

| 점검 | 결과 |
|:--|:--|
| `git check-ignore -q .claude/worktrees/probe` → ignored (아니면 `.gitignore` 또는 `info/exclude`에 `.claude/worktrees/`) | N/A — 이번 작업 트리는 worktree gate가 아니라 별도 clone이다. 공유 기준 트리에서 `info/exclude`로 ignored인 사실은 드라이버가 확인했다. |
| 설정 `worktree.baseRef: "head"` — 에이전트 첫 보고의 `git log --oneline -2`가 기준 HEAD를 보이는가 | N/A — 별도 clone에서는 worktree.baseRef 계약을 적용하지 않는다. 작업 트리 HEAD는 `a13fa48`이고 기준 트리 HEAD도 `a13fa48`이다. |
| 에이전트 첫 보고: 작업 트리 경로 · 브랜치 · HEAD | `/Users/choongjaelee/Codes/terminal-checkout-setup-window-layout-work` · `setup-window-layout-work` · `a13fa48` |
| 트리마다 `uv sync` (기준·작업) | N/A — Python/uv 프로젝트가 아니라 SwiftPM 패키지다. |
| git 밖 로컬 자산을 가리키는 env (이름=절대경로) — 에이전트가 읽기 확인 | `HOME`, `CLAUDE_CODE_EXECPATH`, `CLAUDE_CODE_MESSAGING_SOCKET` 등은 읽혔지만 이 계획의 입력으로 사용하지 않았다. 외부 자산 의존 없음. |
| 증분 리뷰 소요(분) — 첫 세 번 | 미실시 — R0 설계 리뷰 전 |

## 작업 항목

| # | 항목 | 부류 | 확정 결함 | 파일 집합 | 의존 | 상태 | 근거 | 승격 |
|:--|:--|:--|:--|:--|:--|:--|:--|:--|
| 1 | R1에서 최소 Accessibility provider seam과 false→true 권한 전이 재현 fixture만 추가한다 | R1 재현·test seam | (a) `PermissionChecker.isAccessibilityGranted`가 `accessibilityIsTrusted()`를 직접 호출해 기존 테스트가 전이를 만들지 못함 | `app/Sources/App/PermissionChecker.swift`, `app/Tests/AppTests/SetupWindowLayoutTests.swift` | — | verified | 재실행: `cd <clone> && swift test --package-path app --filter SetupWindowLayoutTests` → `testAccessibilityGrantRefreshConvergesWithFittedPlacement`가 provider false→true 전이를 만들고 `accessibilitySection.isHidden` 전후를 단언; 481 tests / 1 skipped / 2 failures (전부 이 케이스) | |
| 2 | R1에서 `settle`을 고정점 반복으로 바꾸고 `layout()` 재진입·pass별 fitting 값을 계측한다 | R1 수렴 계측 | (a) 현재 `settle(_:)`은 `layoutSubtreeIfNeeded()`를 한 번만 호출함 (b) layout 재진입과 pass별 target 변화가 기록되지 않음 | `app/Sources/App/SetupWindowController.swift`, `app/Tests/AppTests/SetupWindowLayoutTests.swift`, `app/Tests/AppTests/SetupWindowRedrawTests.swift`, `app/Tests/AppTests/SetupWindowTestSupport.swift` | 1의 권한 전이 입력 | verified | R3 재실행: 강제 레이아웃 없는 run-loop settle → 7 tests / 8 failures / exit 1. `accessibility-granted`는 `747−592=155pt`, terminal 전이는 127pt red이며, trace에 `clipBounds`·`documentFrame`을 기록한다 (D33). redraw도 같은 helper로 통합한다 (D36) | |
| 3 | R1에서 `8fccab5`의 선행 계약과 override 밖의 `window.screen`·`NSScreen.main` 선택 분기를 Mac Pro + 외장 2대(가로 1, 세로 1)에서 대조한다 | 선행 수정·화면 선택 | — | `app/Sources/App/SetupWindowController.swift`, `app/Tests/AppTests/SetupWindowLayoutTests.swift`, `app/Tests/AppTests/SetupWindowRedrawTests.swift`, `8fccab5` | 1∼2의 재현 계측 | verified | `git show 8fccab5` 대조: visibility 변경 → layout 정착 → fitting 측정과 visible-frame clamp/moveInside를 `FittedContentStackView.layout()`에 모은 계약이 이번 red의 중복 inset은 덮지 않는다. 화면 선택 분기(`window.screen`·`NSScreen.main`)는 자동 테스트에서 여전히 미검증이며, D24 실기기에서는 이번 원인이 아님을 확인했다 | |
| 4 | R1 red 이후에만 잘못된 fixed point의 원천을 수정하고 높이·배치·수렴·scroll restore oracle을 닫는다 | 근본 레이아웃 수정 | (a) 높이만 보는 oracle은 빈 띠를 통과시킴 (b) stack의 38pt 자체 top inset과 `NSScrollView` 자동 32pt title-bar inset이 중복됨 (c) 실기기 154pt 차이는 별도 미해명 (d) `layout()` 안에서 창을 resize하면 scroll view clip이 이전 크기로 남음 | `app/Sources/App/SetupWindowController.swift`, `app/Tests/AppTests/SetupWindowLayoutTests.swift`, `app/Tests/AppTests/SetupWindowRedrawTests.swift`, `app/Tests/AppTests/SetupWindowTestSupport.swift` | 1∼3에서 red 확인 | claimed | R3 driver gate에서 전체 481 tests / 1 skipped / 0 failures로 layout·redraw가 모두 green이다. `SETUP_LAYOUT_SETTLE_WAIT` 4건(그중 `pendingWindowCompletion=true` 2건)을 실제로 지나 fixed-point와 restore completion 대기가 하중을 받았고, `testARebuildReparentsTheFieldAndKeepsTheEditWhereItWas`도 통과해 제품 회귀가 아닌 낡은 하네스 문제로 확정했다 (D39). sync 토글은 21 tests / 12 failures였고, 두 settle 관련 케이스는 green으로 남았다 (D40) | |
| 5 | 4가 green인 뒤 두 재설치·재부여 경로를 실제 화면에서 검증하고 PR 본문 gate로 승계한다 | 기기 TCC·다중 디스플레이 acceptance | — | `install.sh`, `app/Sources/App/PermissionChecker.swift`, `docs/context/signing-and-permissions.md` | 4의 green | verified | 드라이버 기기 acceptance: 권한 없음 794pt / firstInk 40.0pt / lastInk 0.0pt, 허용 후 639pt / firstInk 40.0pt / lastInk 0.0pt; 이전 허용 상태의 firstInk 195.0pt가 사라져 #34 증상을 닫았다. R4의 placeholder 위치 회귀는 별도 재측정 대상이다 (D41) | |

- 항목 하나는 승격 하나에 들어갈 크기다. 같은 부류는 한 승격에 묶이고, 파일 집합이 겹치지 않는 부류만 따로 승격할 수 있다. 승격 칸에는 커밋 해시를 적는다.
- `의존`: 2와 3은 1의 권한 전이를 전제로 한다. 4는 1∼3을 적용한 현재 tree에서 red가 확인된 뒤에만 시작한다. 5는 4의 자동 gate가 green인 뒤에만 수행한다.
- R1 첫 배정은 1∼3의 재현·계측뿐이다. provider seam은 red를 만들기 위한 최소 변경이고, 반복 settle과 pass trace는 관찰 장치다. R1에서 red가 없으면 그 사실과 실기기와 다른 경계를 보고하고 멈추며, 4의 production layout fix는 배정하지 않는다.
- `확정 결함`: 레이아웃 최초 원인인 (a) 조기 `fittingSize` 측정, (b) `lastRequestedSize` early return, (c) non-flipped document 배치는 아직 확정하지 않는다. 표에는 현재 코드와 사용자 관측으로 확정된 테스트 공백·높이 불일치만 적고, R0 실측 후 결함 문구를 개정한다.
- 판정이 항목을 다시 열면 행을 고치지 말고 개정 항목으로 잇는다 — 같은 항목의 개정은 prime(`6′`), 형제 부류나 prime 소진 뒤는 letter(`6a`), 식별자는 재사용하지 않는다. 선행 행의 상태는 그 승격에 대한 마지막 판정이지 종결이 아니다.
- 상태 사다리: `todo` → `wip` → `claimed` → `verified` → `cleared` → `agreed`. 이탈은 `dropped`.
- `claimed`까지가 구현 에이전트가 스스로 올리는 상한이다. `verified`·`cleared`·`agreed`·`dropped`는 드라이버의 결정이다.
- 근거 칸에는 재실행 가능한 명령과 결과 줄, 테스트 이름과 수, 수치를 낸 파일 경로만 적는다. 이번 게이트의 환경 실패는 회귀 red가 아니다.

## 결정 원장

| # | 유형 | 주장/위험 | 결정 | 근거 (명령·수치·경로 · SHA 또는 리뷰 번호) | 잔여 불확실성 |
|:--|:--|:--|:--|:--|:--|
| D1 | 드라이버 | 이번 배정은 구현 계획 초안까지이며 코드 수정·테스트 수정·커밋을 금지한다 | 계획 파일만 작성하고 멈춘다 | 사용자 배정문; 작업 트리 `git status --short --branch`가 `## setup-window-layout-work`만 출력 | 드라이버가 후속 구현 승격을 정하지 않음 |
| D2 | 드라이버 | 완료는 권한 false→true 전이를 포함해야 하며 기존 초록 테스트가 현실과 다른 이유를 밝혀야 한다 | live TCC를 테스트에서 뒤집지 않고 provider seam을 항목 1로 둔다 | `PermissionChecker.swift:49∼61`; `SetupWindowLayoutTests.swift:203∼214` | seam의 최종 이름과 주입 위치는 구현 리뷰에서 결정 |
| D3 | 드라이버 | 빈 띠와 상단 클리핑은 한 부류의 양면인지 확인해야 한다 | 창 content height와 document fitting height의 양방향 equality/clamp를 공통 oracle로 삼고, non-flipped 위치를 별도 관찰한다 | `SetupWindowController.swift:15∼56`, `315∼353`; 이슈 #34 관측 | 최초 원인이 측정 순서·early return·scroll 배치 중 무엇인지는 red 전까지 open |
| D4 | 드라이버 | `fittingSize` 조기 측정, `lastRequestedSize` early return, documentView 배치가 원인일 수 있다 | 읽기 가설으로만 유지하고 구현 결론으로 승격하지 않는다 | 사용자 배정문의 읽기 전용 가설; 선행 수정 [8fccab5](https://github.com/dazebug/terminal-checkout/commit/8fccab5) | headless 테스트가 환경 실패해 이번 라운드에는 동적 수치가 없음 |
| D5 | 드라이버 | 이 환경의 게이트 실패는 구현 회귀로 반올림하면 안 된다 | 두 실행을 `환경 실패·판정 보류`로 기록하고 driver 환경에서 재실행한다 | `swift test --package-path app` → exit 1, clang ModuleCache `Operation not permitted`; cache env 재시도 → exit 1, `sandbox-exec: sandbox_apply: Operation not permitted` | Xcode/SwiftPM 실행 가능한 검증 환경에서 테스트 수와 red→green 필요 |
| D6 | 드라이버 | 높이 equality만으로는 짧은 document가 clip 아래에 놓이는 빈 띠와 top card clipping을 잡지 못한다 | `documentVisibleRect.maxY`·document `frame.maxY`, `card.header`의 viewport 포함, short-document의 양끝 일치를 별도 placement oracle로 둔다 | 이슈 #34의 배치 증상; `SetupWindowController.swift:322∼330`; 반박 1 | 실제 AppKit 좌표 tolerance와 header의 초기 top staging은 driver 환경에서 측정 |
| D7 | 드라이버 | `setContentSize`가 `layout()` 안에서 재진입하고 `lastRequestedSize`가 fixed point를 고정한다면 빈 띠·클리핑은 같은 부류일 수 있다 | R1에서 settle을 bounded fixed-point loop로 만들고 pass별 layout/fitting/target/frame trace를 낸다. red 전에는 수정 방향을 고르지 않는다 | `SetupWindowController.swift:47∼56`; `SetupWindowLayoutTests.swift:56∼58`; 반박 2 | 실제 재진입 횟수와 pass별 수치 미측정 |
| D8 | 드라이버 | override 밖의 `window.screen`·`NSScreen.main` 선택은 다른 앱에서 돌아오는 다중 디스플레이 경로에서 미검증이다 | R1 계측 뒤 화면 선택을 이름 있는 함수로 분리해 단위 테스트에 닿게 할지 결정하고, 어느 경우에도 소탕 표와 device display sweep을 남긴다 | `SetupWindowController.swift:50∼52`; 반박 3 | 실제 `window.screen` 선택과 screen별 clamp 수치는 미측정이며, 디스플레이 구성은 D15에서 확인했다 |
| D9 | 드라이버 | `8fccab5`의 visibility→settle→measure 계약이 이번 권한 text/hidden 전이·재진입·placement를 덮었는지는 확인되지 않았다 | `git show 8fccab5`로 선행 계약과 이번 입력의 틈을 먼저 대조하고, 같은 자리에 수동 resize를 추가하는 point fix는 red 후에만 검토한다 | `8fccab5`; `SetupWindowController.swift:15∼56`; 반박 4 | 선행 계약의 실제 누락 축은 R1 trace 전 open |
| D10 | 드라이버 | R1에서 red가 없으면 항목 4의 수정 근거가 없다 | R1 첫 배정은 재현·계측만 하고, red가 없으면 실기기와 테스트 모델의 차이를 보고한 뒤 멈춘다 | 작업 항목 1∼3; 반박 5 | red를 만들 최소 provider seam의 최종 shape 미결정 |
| D11 | 사용자 | 실제 사용자가 본 권한 부여와 재설치 후 재부여 두 경로가 종결 조건이다 | 지정된 두 기기 절차를 완료의 정의에 고정하고, 결과를 PR 본문으로 승계한다 | 이슈 #34 코멘트 2·4; 반박 6 | 첫 기기 재현의 실행 시점은 D14에서 R1 재현 red 직후 한 번으로 확정 |
| D12 | 드라이버 | clone에서의 worktree ignore/baseRef 점검은 기준 worktree 사실과 섞이면 오해를 만든다 | 이 계획의 두 배치 점검 행은 N/A로 두고, 기준 트리의 shared `info/exclude` 확인은 드라이버 사실로만 기록한다 | 반박 7; 기준 트리 `git check-ignore` 결과 | spark 변형의 실제 checkout 형태가 바뀌면 점검 문구 재검토 |
| D13 | 드라이버 | raw `rg` match와 value-flow inventory를 같은 수치로 쓰면 재현 근거가 흔들린다 | 계획에는 `rg -n "refresh\\(\\)" ... | wc -l` → 23(주석·정의 포함)을 쓰고, 실제 값의 흐름은 전수 표에서 열거한다 | 반박 8; `SetupWindowController.swift`의 raw match 명령 | 주석·정의 제외의 별도 call-site count는 R1에서 필요할 때만 추가 |
| D14 | 사용자 | 기기 재현(`tccutil reset` → 부여 → 창 확인)의 실행 시점은 R1 재현 red 직후 한 번이다 | 테스트 모델이 현실과 어긋났는지를 수정 방향을 고르기 전에 확인하도록, 해당 기기 재현을 R1 red 직후 한 번 실행한다 | 사용자 답변; D11; 완료의 정의 기기 acceptance 1 | red 직후의 실제 TCC 상태와 테스트 red의 모양이 얼마나 대응하는지는 기기 실행에서 확인 |
| D15 | 사용자 | 디스플레이 구성은 Mac Pro + 외장 2대(가로 1, 세로 1)다. 세로 디스플레이는 `visibleFrame.height`가 가로와 크게 다르므로 D8의 화면 선택 분기는 실재 위험이다 | 항목 3의 선행 계약 대조에 `window.screen`·`NSScreen.main`의 다중 디스플레이 선택과 screen별 clamp를 명시한다 | 사용자 답변; 항목 3; D8 | 실제 창이 어느 화면에 속하고 System Settings 왕복 뒤 선택이 어떻게 바뀌는지는 R1/device sweep에서 측정 |
| D16 | 드라이버 | 반박 1이 실측으로 확정됐다. 같은 recipe의 비뒤집힌 `NSStackView` documentView는 짧은 콘텐츠에서 높이 oracle만으로 빈 띠를 통과시킨다 | `swift clipgeom.swift`의 기하를 placement oracle의 실측 근거로 채택한다. 콘텐츠 320pt·창 800pt에서 `stack.frame = (0, 480, 560, 320)`, `clip.bounds = (0, 480, 600, 800)`이 되어 `topAnchor` 제약이 document를 위로 붙이지 않고 clip bounds origin을 옮기며, 뷰포트 위 480pt가 빈다. 콘텐츠 920pt·창 400pt에서는 `stack.frame = (0, -520, …)`, `clip.bounds = (0, 0, 600, 400)`으로 document 상단이 보인다. 따라서 D6의 placement oracle은 실측 근거를 얻었고, 이 기하만으로는 상단 클리핑을 설명할 수 없으므로 D7의 단일 부류 해석과 실제 다중 패스 거동은 R1에서 계속 검증한다 | `/private/tmp/claude-501/-Users-choongjaelee-Codes-terminal-checkout/4ffc53dd-bc2a-45a7-9448-a47a2106d5f6/scratchpad/clipgeom.swift`; `swift clipgeom.swift`; 드라이버 실측; D6·D7 | 스크립트는 run loop 없이 `layoutSubtreeIfNeeded()`로만 정착시켜 실제 앱의 다중 패스 거동은 재현하지 않는다 |
| D17 | 드라이버 | clone의 커밋 identity가 회사 계정으로 나갔다 | 이 clone의 `user.name`·`user.email`을 dazebug로 고정하고 `git reset --soft HEAD~1` 후 새 커밋으로 재발행한다. amend는 사용하지 않는다 | 기존 `858f708` author `tim-watcha <tim@watcha.com>`; 현재 `git config user.email` → `1300067+dazebug@users.noreply.github.com`; 기준 트리 `git config user.email`도 후자; 드라이버가 clone config를 교정함 | 이후 라운드의 커밋 author를 매 승격마다 드라이버가 확인한다 |
| D18 | 드라이버 | 고정 강제 레이아웃 복제에서 높이 equality와 2-pass 수렴은 모든 hidden 전이에서 유지됐지만, 축소 때만 document 위에 빈 띠가 남는다 | D6의 `visible.maxY == document.frame.maxY`를 축소 빈 띠의 placement oracle로 유지한다. `visible.minY == document.frame.minY`는 document가 clip보다 짧거나 같은 경우에만 단언하고, document가 더 긴 scroll 상태에서는 검사하지 않는다. 이 모형에서는 `lastRequestedSize` early return·layout 재진입 발산을 원인으로 채택하지 않는다 | `/private/tmp/claude-501/-Users-choongjaelee-Codes-terminal-checkout/4ffc53dd-bc2a-45a7-9448-a47a2106d5f6/scratchpad/fittedloop.swift`; `swift fittedloop.swift`; 초기 622/622/18pt, hidden 494/494/32pt, visible 622/622/0pt, 반복 전이 동일; settle 2회, 재진입 없음, early return 없음 | run loop 없는 복제이므로 실제 앱의 다중 패스와 권한 전이 경계는 미측정이며, D4·D7 가설은 실제 앱에서 완전히 닫히지 않는다 |
| D19 | 드라이버 | R1 red 확정. Accessibility 허용 전이 뒤 신규 placement oracle이 두 방향으로 실패한다 | 전체 게이트가 exit 1인 의도된 재현 red로 남긴다. denied에서 granted로 줄어든 뒤 clip bounds origin이 32.0에 남은 상태를 항목 4의 production 수정 전제와 항목 5의 기기 검증 입력으로 승격한다 | `/private/tmp/claude-501/-Users-choongjaelee-Codes-terminal-checkout/4ffc53dd-bc2a-45a7-9448-a47a2106d5f6/tasks/b2sapk19c.output`; `swift test --package-path app` exit 1; `XCTAssertEqualWithAccuracy` 두 실패: `779.0`은 `747.0` ±`0.5` (`accessibility-granted: the viewport top extends above the document`), `32.0`은 `0.0` ±`0.5` (`accessibility-granted: the viewport has a blank region below the document`); `SETUP_LAYOUT_SNAPSHOT[accessibility-denied] contentSize=(600.0, 902.0) fittingSize=(600.0, 902.0) rootFrame=(0.0, 0.0, 600.0, 902.0) contentBounds=(0.0, 0.0, 600.0, 902.0) documentVisibleRect=(0.0, 0.0, 600.0, 902.0) rootMaxY=902.0`; `SETUP_LAYOUT_SNAPSHOT[accessibility-granted] contentSize=(600.0, 747.0) fittingSize=(600.0, 747.0) rootFrame=(0.0, 0.0, 600.0, 747.0) contentBounds=(0.0, 32.0, 600.0, 747.0) documentVisibleRect=(0.0, 32.0, 600.0, 747.0) rootMaxY=747.0`; `SETUP_LAYOUT_SNAPSHOT[accessibility-granted-repeat] contentSize=(600.0, 747.0) fittingSize=(600.0, 747.0) rootFrame=(0.0, 0.0, 600.0, 747.0) contentBounds=(0.0, 32.0, 600.0, 747.0) documentVisibleRect=(0.0, 32.0, 600.0, 747.0) rootMaxY=747.0`; `SETUP_LAYOUT_TRACE[accessibility-denied] layoutPasses=1`, `pass=1 fittingHeight=902.0 targetHeight=902.0 lastRequestedHeight=902.0 appliedContentHeight=902.0 requested=false`; `SETUP_LAYOUT_TRACE[accessibility-granted] layoutPasses=1`, `pass=1 fittingHeight=747.0 targetHeight=747.0 lastRequestedHeight=747.0 appliedContentHeight=747.0 requested=true`; `SETUP_LAYOUT_TRACE[accessibility-granted-repeat] layoutPasses=0`; 전체 481 tests / 1 skipped / 2 failures | 테스트 빈 띠 32.0pt가 사용자 실측 250pt·55%와 다른 이유는 D21로 남긴다 |
| D20 | 드라이버 | D4(a) `fittingSize` 조기 측정과 D4(b) `lastRequestedSize` early return, D7의 되먹임 루프가 이 입력의 크기 축 원인이라는 가설 | 이 입력의 크기 축에서는 세 가설을 기각한다. layout은 한 pass이고 재진입이 없으며 `fitting == target == applied == 747.0`, 두 번째 settle은 `layoutPasses=0`이다. 결함 축을 clip bounds origin의 보존으로 좁힌다 | D19 원문 trace; granted snapshot `contentSize=(600.0, 747.0)`, `fittingSize=(600.0, 747.0)`, `contentBounds=(0.0, 32.0, 600.0, 747.0)`, `documentVisibleRect=(0.0, 32.0, 600.0, 747.0)` | 이 결론은 현재 테스트 모델의 크기 축에 한정하며, 실제 기기의 250pt·55% 모양과 화면 선택 분기는 아직 닫히지 않았다 |
| D21 | 드라이버 | 테스트에서 재현된 빈 띠 32pt와 사용자 보고 250pt·창의 55% 사이에 크기 차이가 있다 | 부류는 재현했으나 크기는 재현하지 못했다. 원인과 수치를 항목 5 기기 검증 전까지 열린 상태로 둔다 | D19의 `contentBounds.origin.y=32.0`; 이슈 #34 코멘트 1의 사용자 실측 약 55% 빈 영역·코멘트 본문의 약 250pt | 다중 디스플레이 화면 선택, 실제 창 크기, run loop와 설치·권한 왕복이 차이를 만드는지는 기기 acceptance에서 확인한다 |
| D22 | 드라이버 | 테스트가 헤더를 찾기 위해 production `NSView` 확장의 가시성을 넓히는 것은 불필요한 경계 변경이다 | `private extension NSView`를 유지하고 `card.header`는 `rootStack.arrangedSubviews`에서 직접 찾는다. 자손 탐색을 공유 API로 만들지 않는다 | `SetupWindowController.swift:1502`; `buildContent()`의 카드 목록에서 `card.header`가 root stack의 직접 arranged subview임; R1 리뷰 1 지적 | 헤더 식별자와 arranged-subview 순서는 현재 `buildContent()` 계약에 의존하며 이번 배정에서 변경하지 않는다 |
| D23 | 드라이버 | 실기기에서도 권한 허용 직후 창이 축소되며 빈 띠와 하단 clipping이 함께 생기고, 두 번째 refresh로 자가 치유되지 않는다 | R1 단위 red의 32pt는 실기기 154pt의 약한 대리 신호로 취급한다. `794 − 639 = 155pt` 축소량과 거의 같은 154pt가 콘텐츠를 아래로 밀고 하단을 자르므로, 수정은 32pt를 맞추지 않고 축소 뒤 clip bounds origin을 새 document 경계에 맞추는 메커니즘을 대상으로 한다 | 절차 `tccutil reset Accessibility com.dazebug.terminal-checkout` → 앱 재시작 → 정상 창 확인 → [손쉬운 사용 권한 요청] AX 클릭 → System Settings에서 허용 → 창 관측; `/private/tmp/claude-501/-Users-choongjaelee-Codes-terminal-checkout/4ffc53dd-bc2a-45a7-9448-a47a2106d5f6/scratchpad/winprobe.swift`, `firstink.swift`; 초기(부여됨) 창 639pt·첫 픽셀 41.0pt 정상, reset+재시작(거부됨) 794pt·40.0pt 정상, 허용 직후 639pt·195.0pt 깨짐, 재활성화 후 639pt·195.0pt 깨짐; 콘텐츠가 154pt 아래로 밀리고 빈 띠는 축소량 전체이며 마지막 픽셀이 하단 경계에 닿아 하단 154pt가 잘림 | 실제 창·스크린샷 계측은 AppKit constraint 로그를 직접 보여주지 않으며, 단위 32pt와 기기 154pt의 차이는 강제 layout 반복과 실제 display cycle의 차이를 포함할 수 있다 |
| D24 | 드라이버 | 사용자의 다중 디스플레이 화면 선택이 이번 빈 띠의 원인일 수 있다 | 이번 증상의 원인으로는 기각한다. 콘텐츠 최대 794pt가 두 화면의 visible height보다 작아 clamp가 작동하지 않았으므로, 항목 4는 화면 선택 분기를 수정하지 않는다. 다만 자동 테스트가 override만 사용해 실제 선택 분기를 지나지 않는 사실은 소탕 표에 남긴다 | `winprobe.swift` 화면 계측: `LG UltraFine (1) 세로 frame=(0,0,1440,2560) visibleFrame=(0,0,1440,2530)`, `LG UltraFine (2) 가로 frame=(-2560,468,2560,1440) visibleFrame=(-2560,468,2560,1440)`, `NSScreen.main = LG UltraFine (1)` 세로, 설정 창 X=`-1580` 가로 화면; D23 콘텐츠 최대 794pt | 실제 화면 선택이 자동 테스트에서 미검증인 상태는 남아 있으며, 별도 display sweep의 부류로만 추적한다 |
| D25 | 구현자 | `stack.topAnchor == scroll.contentView.topAnchor`가 `stack.frame.maxY=747`과 `documentVisibleRect.maxY=779`의 불일치를 막지 못한 이유와 세 대안의 범위가 필요하다 | 메커니즘 판단: Auto Layout의 `topAnchor`는 view frame edge를 정하고, `NSClipView`의 bounds origin은 scroll position으로 별도 관리되므로 이 수치는 constraint break가 아니라 frame 배치와 visible document 좌표의 분리다. 구현 제안은 (c) `setContentSize` 뒤 현재 bounds origin을 document/clip의 유효 범위에만 clamp하는 것 — 화면보다 긴 document에서는 현재 scroll을 보존하고, `restore(place:)`가 지정한 유효 anchor는 덮어쓰지 않는다. (a)는 비뒤집힌 `maxY` 계약을 뒤집어야 하므로 기각·승인 대기, (b)는 `constrainBoundsRect(_:)`가 이 resize 경계에서 호출된다는 증거가 없고 restore와의 상호작용을 별도 계측해야 하므로 이번 구현에서는 채택하지 않는다 | `SetupWindowController.swift:57∼75`는 `super.layout()` 후 `window.setContentSize(target)`만 하고 clip origin을 보정하지 않으며, `buildContent()`의 제약은 `SetupWindowController.swift:449∼453`; R1 granted snapshot `stack.frame=(0,0,600,747)`, `contentBounds=(0,32,600,747)`, `documentVisibleRect=(0,32,600,747)`, repeat `layoutPasses=0`; Apple `NSView.topAnchor`는 frame edge, `NSClipView`는 bounds/documentVisibleRect/scroll을 관리하고 `constrainBoundsRect(_:)`는 proposed bounds를 제한한다 | (c)의 범위 clamp 공식과 `constrainBoundsRect` 호출 시점은 구현 후 short/equal/tall document, shrink/grow, user scroll, language restore 테스트로 확인해야 하며, `layoutProbeForTesting`는 그 수렴 수치를 확인한 뒤 제거 여부를 드라이버가 결정한다 |
| D26 | 사용자 | "베스트프랙티스대로 해" — 원천 수정을 우선하고, 필요하면 비목표를 바꿀 권한까지 준다. 이 결정으로 flipped 전환의 범위 변경도 사전 승인됐으나, 드라이버 실측(B ≡ D)이 flipped가 사는 것이 없음을 보여 채택하지 않았다 | 비목표의 `capturePlace`·`restore` 비뒤집힌 `maxY` 계약을 유지한다 | 사용자 지시; D27의 B·D 대조 | flipped 전환은 이번 결함에서 닫히지 않았으며, 이후 다른 결함이 입증될 때 별도 범위로 다시 연다 |
| D27 | 드라이버 | 중복 content inset이 R1의 32pt 빈 띠를 만들며, flippedness가 결정 변수가 아닐 수 있다 | `buildContent()`에서 `scroll.automaticallyAdjustsContentInsets = false`를 적용한다. `.fullSizeContentView`의 자동 title-bar inset 32pt와 stack의 `edgeInsets.top=38`이 중복되고, 원인을 제거했으므로 (a) flipped document, (b) `NSClipView.constrainBoundsRect` 서브클래스, (c) resize 후 origin clamp는 모두 이번 수정에서 기각한다. 실기기 154pt는 이 변경으로 닫힌다고 주장하지 않는다 | `/private/tmp/claude-501/-Users-choongjaelee-Codes-terminal-checkout/4ffc53dd-bc2a-45a7-9448-a47a2106d5f6/scratchpad/insetprobe.swift`, `combo.swift`; A auto-on: initial/shrink `clip.origin.y=32.0`, `insets.top=32.0`, `blankAbove=32.0`, grow origin 0; B unflipped/auto-off: 모든 전이 origin/inset/blank 0; C flipped/auto-on: origin -32/inset 32/blank 32; D flipped/auto-off: 모두 0; D′/B′ 300pt 화면에서는 둘 다 scrollable=true·FIRSTCARD_VISIBLE=true | R1 단위 32pt와 실기기 154pt의 차이는 남은 별건 원인 후보이며, 드라이버 기기 재검증 전에는 추측하지 않는다 |
| D28 | 구현자 | D25가 `constrainBoundsRect(_:)`를 기각한 이유로 적은 “resize 경계 호출 여부 미검증”은 이번 결정의 근거가 될 수 없다 | 그 미검증 주장은 철회한다. (b)는 `automaticallyAdjustsContentInsets` 중복을 제거하면 보정할 결함이 남지 않으므로 불필요하다는 D27의 원인 제거 근거로만 기각한다 | D27; 사용자 지시의 대안 기각 조건 | `constrainBoundsRect(_:)` 호출 시점은 이번 계획에서 결론 내리지 않으며, 실기기 154pt가 남을 때 별건 원인 조사에서 다시 계측한다 |
| D29 | 드라이버 | 토글 증명 결과. 자동 inset 제거를 역적용한 상태에서 설정 전이·터미널 전이의 축소 방향 placement oracle만 red가 된다 | 이 수정의 검증 단언은 축소 방향에 있음을 확정한다. 권한 부여 2건, 터미널 전이 8건, 설정 되읽기 1건이 실패했고, 권한 역전·bottom-edge 성장·긴 문서 정지 상태는 red가 아니다 | `/private/tmp/claude-501/-Users-choongjaelee-Codes-terminal-checkout/4ffc53dd-bc2a-45a7-9448-a47a2106d5f6/tasks/b2wz3kgrr.output`; 481 tests / 1 skipped / 11 failures / exit 1; 라벨 집계 `accessibility-granted:` 2건, `accessibility-denied-again`·`terminal-growth`·`tall-document-rest` 0건 | |
| D30 | 드라이버 | `XCTAssertFalse(automaticallyAdjustsContentInsets)`는 설정 되읽기이며 같은 토글에서 red가 되는 배치 단언과 결과가 겹친다 | 해당 단언과 테스트에서만 쓰던 `scroll` 지역 변수를 제거한다. 동작이 아닌 구현 선택을 고정하지 않으며, 계약 설명은 production 주석에 남긴다 | 토글 원문; `SetupWindowLayoutTests.swift`의 `accessibility-granted` 배치 실패와 설정 되읽기 실패 | |
| D31 | 드라이버 | `assertFittedPlacement`가 네 입력 지점에서 호출된다 | 현재는 권한 부여 축소·권한 역전 확대·터미널 전이·bottom-edge 성장이라는 서로 다른 입력의 매개변수화로 유지한다. 종결 전 테스트 심사에서 경계당 대역 수를 재판정한다 | `SetupWindowLayoutTests.swift:315`, `338`, `406`, `424`; `rg -n "assertFittedPlacement" app/Tests/AppTests/SetupWindowLayoutTests.swift` → helper 정의 1건과 호출 4건 | |
| D32 | 드라이버 | R2의 자동 content inset 제거가 실기기 증상을 바꾸지 않았다. 실제 원인은 `layout()` 안에서 window를 resize한 뒤 scroll view가 이전 크기에 남는 것이다 | 측정은 `layout()` 안에 두되 `setContentSize`와 `moveInside` 적용을 다음 main-queue turn으로 미룬다. `tile()`과 `needsLayout = true`는 같은 stale clip을 남겨 기각하며, async 적용만 `win=702`·`clip=702`와 확대 `BLANK_ABOVE=0`을 만들었다. `NOINSET=0`에서 async만 쓰면 32pt가 남아 두 원인은 독립이다. clamp 경로는 async 여부와 무관하게 회귀하지 않는다 | `/private/tmp/claude-501/-Users-choongjaelee-Codes-terminal-checkout/4ffc53dd-bc2a-45a7-9448-a47a2106d5f6/scratchpad/runloop.swift`; `FIX=none|tile|dirty|async|asynctile`, `NOINSET=0|1`, `SCREEN=<pt>` 대조; `none` 축소 `win=702`, `clip=547`, 확대 `BLANK_ABOVE=155`; `tile`·`dirty`도 동일; `async` 축소 clip 702·확대 blank 0; `async + NOINSET=0` blank 32; `SCREEN=500`은 async 여부와 무관하게 clip 500·scrollable | `rebuildForLanguageChange()`의 restore와 초기 센터링은 async 적용 순서에 의존하므로 R3 구현·redraw gate에서 별도로 검증한다 |
| D33 | 드라이버 | 강제 레이아웃 없는 충실한 run-loop settle이 실기기와 같은 결함을 단위 테스트에서 red로 재현했다 | `testAccessibilityGrantRefreshConvergesWithFittedPlacement`와 terminal 전이 placement가 실패하는 것이 기대된 red다. 강제 layout을 되살리지 않고 async production fix 후 같은 oracle을 green으로 만든다 | `swift test --package-path app --filter SetupWindowLayoutTests`; 7 tests / 8 failures / exit 1; `accessibility-granted` `747−592=155pt`, `accessibility-denied-again` 155pt, terminal 전이 127pt, `tall-document-rest`의 `709 < 745.5`; 기존 강제 settle에서는 6개가 초록이었다 | 하네스가 실제 display cycle을 모델링하기 전에는 `assertFirstCardVisible`이 구 코드에서도 통과해 보였다 |
| D34 | 드라이버 | 하네스 충실도가 단언의 분류를 바꾼다 | `assertFirstCardVisible`은 R2 토글에서 (ii)로 보였지만, 강제 레이아웃 없는 R3에서 red가 되어 이 수정의 (i) oracle이기도 하다. 구 코드에서 통과한다는 분류를 폐기하고 #34 상단 클리핑 특성화와 토글 검증 모두로 유지한다 | D33 원문; `SetupWindowLayoutTests.swift:219` `tall-document-rest` 실패 | |
| D35 | 구현자 | production에서 창이 닫힌 뒤 다시 표시될 때 `resumeDeferredWindowUpdates`가 `windowDidBecomeKey`에만 있는 것이 누락 경로를 만들 수 있는가 | AppDelegate의 모든 표시 진입점이 `showWindow(nil)` 뒤 `makeKeyAndOrderFront(nil)`을 호출하므로 현재 production에는 `orderFront`만으로 재표시하는 경로가 없다. 재개 지점은 `windowDidBecomeKey`에 유지한다 | `AppDelegate.swift:16`, `30`, `35`, `111∼122`; `rg -n "showSetupWindow\(|showWindow\(|makeKeyAndOrderFront\(" app/Sources/App`; `applicationShouldHandleReopen`·launch·guidance callback 모두 `showSetupWindow()`으로 수렴 | 향후 새 표시 경로가 key를 만들지 않는다면 공통 show 경계에 재개를 추가해야 한다 |
| D36 | 드라이버 | R3 async 수정 뒤 layout tests는 green인데 redraw 3건이 남았다 | `makeScrollingController()`와 `post(_:)`의 시간·강제 레이아웃 하네스를 폐기하고 `SetupWindowTestSupport.settle` 하나로 통합한다. fixture가 적용한 window content height와 clip height를 출력·단언해 stale placeholder를 통과시키지 않는다 | `/private/tmp/claude-501/-Users-choongjaelee-Codes-terminal-checkout/4ffc53dd-bc2a-45a7-9448-a47a2106d5f6/tasks/b5zh73ry2.output`; `SetupWindowLayoutTests` 7 tests / 0 failures; 전체 481 tests / 1 skipped / 3 failures; `SetupWindowRedrawTests.swift:291`, `313`, `324`; 기존 fixture의 `layoutSubtreeIfNeeded()` 두 곳과 `post`의 0.05초 pump | 공용 settle 후 실제 fixture 수치는 다음 driver gate의 `SETUP_REDRAW_FIXTURE` trace로 확정한다 |
| D37 | 구현자 | deferred application 뒤 `FittedContentLayoutPass.appliedContentSize`라는 이름은 측정 시점의 이전 크기를 실제 적용 크기로 오해하게 한다 | 필드와 trace label을 `contentSizeBeforeApplication`으로 바꾸고, 스크롤뷰가 없을 때 `documentFrame`은 stack frame을 대신하지 않는 optional로 둔다 | `SetupWindowController.swift:17∼24`, `145∼158`; `SetupWindowLayoutTests.swift:65∼78` | deferred block 실행 후의 실제 크기는 snapshot이 증언하며 layout-pass trace는 측정 시점 값으로 한정한다 |
| D38 | 구현자 | init placeholder frame이 최초 deferred size application 전에 한 display cycle 보일 수 있다 | 적용을 `layout()` 안으로 되돌리지 않고, 기기에서 보이면 최초 size-application completion 뒤에만 표시하거나 alpha를 복원하는 완화를 검토한다 | `SetupWindowController.swift:311∼317`; AppDelegate는 `showWindow`·`makeKeyAndOrderFront`를 즉시 호출한다 (`AppDelegate.swift:111∼122`) | 실제 표시 여부와 사용자가 보는 점프 크기는 드라이버 기기 검증 전 미측정 |
| D39 | 드라이버 | R3 공용 run-loop settle 뒤 redraw의 field-editor selection 실패가 하네스 문제인지 제품 회귀인지 판정해야 한다 | `SETUP_LAYOUT_SETTLE_WAIT` 4건 중 2건이 `pendingWindowCompletion=true`였고, settle이 restore completion을 기다린 뒤 `testARebuildReparentsTheFieldAndKeepsTheEditWhereItWas`가 통과했다. 따라서 `restore(place:)`의 제품 회귀가 아니라 completion을 기다리지 않던 하네스 문제로 확정한다 | 드라이버 전체 gate: exit 0, 481 tests / 1 skipped / 0 failures; `SETUP_LAYOUT_SETTLE_WAIT` 4건, 그중 completion pending 2건; 해당 테스트 0.335s에 통과 | 실제 기기에서 첫 표시 placeholder가 보이는지와 layoutProbe의 종결 처분은 별도 미결 |
| D40 | 드라이버 | R3 sync 토글에서 통과한 두 케이스의 분류와 D31의 네 호출 지점 판정을 닫아야 한다 | `testTheWindowFitsItsContentInEveryPopulatedLocale`(0.797s)와 `testARebuildReparentsTheFieldAndKeepsTheEditWhereItWas`(0.294s)는 sync 토글에서도 green이므로 (ii)다. 앞선 red는 async 적용이 아니라 settle hole이었다. 이는 R2의 `assertFirstCardVisible`에 이은 두 번째 하네스 충실도 재분류다. D31은 `assertFittedPlacement` 네 호출을 권한 부여 축소·터미널 전이 축소 (i), 권한 역전 확대·bottom-edge 성장 (ii)로 닫으며, 서로 다른 입력 경계라 중복 제거하지 않는다. | sync 토글 gate exit 1, 21 tests / 12 failures; `assertFittedPlacement` 호출 지점 `SetupWindowLayoutTests.swift:242`, `265`, `333`, `351`; `SETUP_LAYOUT_SETTLE_RETURN`은 제거하고 실제 pending을 보여 준 `WAIT`만 유지하며, `layoutProbeForTesting`은 기기 acceptance까지 유지한다 | 최종 종결 전 driver가 layoutProbe 처분과 기기 acceptance를 결정한다 |
| D41 | 드라이버 | 기기 acceptance는 #34 레이아웃을 닫았고 `layoutProbeForTesting`은 유지하기로 결정했으며, R3가 만든 placeholder 위치 점프를 별도 회귀로 확인했다 | 기기에서 권한 없음 794pt / firstInk 40.0pt / lastInk 0.0pt, 허용 후 639pt / firstInk 40.0pt / lastInk 0.0pt로 빈 띠·상단 clipping은 사라졌다. `layoutProbeForTesting`은 세 번 재발한 clip/document 불일치를 특정한 관측 장치이므로 유지한다. 새 위치 회귀는 init center를 복원하고 첫 deferred center는 유지한다. | `scratchpad/jumpwatch.swift`의 2ms `CGWindowListCopyWindowInfo` 폴링: init center 제거 시 `X=0 Y=1920 W=600 H=640`이 약 75ms 보인 뒤 중앙으로 이동; 사용자의 터미널 선택은 변경하지 않았다 | init center 복원 후 같은 기기에서 위치 점프가 사라지는지와 다음 gate를 재확인한다 |
| D42 | 드라이버 | R4의 init center 복원은 placeholder 위치 점프를 없앴지만, `center()`와 layout의 visible-frame clamp가 서로 다른 화면을 읽어 최종 창을 좌단에 놓을 수 있다 | 화면은 생성 직후 `NSScreen.main`을 한 번 읽어 placeholder를 배치하고, 이후 layout cycle이 캡처한 하나의 visible rect만 `centerInside`와 `moveInside`에 사용한다. `NSWindow.center()`는 제거하며, visible rect가 바뀌어 target height가 같아도 새 배치를 예약한다 | `scratchpad/centerprobe.swift`: 세로 `visibleFrame=(0,0,1440,2530)`, 가로 `visibleFrame=(-2560,468,2560,1410)`, `NSScreen.main`은 세로; placeholder center와 deferred center는 가로를 선택한 뒤 `moveInside`가 세로 rect로 X=0에 clamp되는 순서와 일치한다. R4 기기 최종 frame은 `X=0`, 600×794였다 | 새 visible-frame placement test의 red와 수정 후 전체 gate·기기 위치 재측정이 남아 있다 |
| D43 | 드라이버 | R5의 visible-frame placement test는 두 번째 override cycle에서 `centerAfterFirstWindowUpdate`가 false인 상태에 다시 center를 요구해 제품 계약을 잘못 단언했다. 두 화면의 실제 불일치는 단위 테스트에서 red로 만들 수 없다 | 해당 테스트를 삭제하고 `testCenteringAndClampingConsumeTheSameVisibleFrame`로 대체한다. 같은 rect의 `centerInside` 뒤 `moveInside`는 중앙을 보존하고, 다른 rect의 `moveInside`는 edge로 clamp되는 순수 산술만 고정한다. 이 테스트는 (ii) 불변 원칙이며 구 코드의 두 화면 증상을 (i) red로 증명하지 않는다. 두 화면 선택과 실제 X=0은 기기 acceptance에 남긴다 | R5 gate exit 1, 482 tests / 1 skipped / 2 failures; 신규 테스트의 `2400.0` 대 `2900.0`, `1747.5` 대 `2374.0` 실패는 `visible.minX + window.width/2`인 edge clamp를 보였다. 기존 481 tests는 모두 green이었다 | 순수 산술 테스트가 compile·green인지와 `window.screen` nil 분기의 실제 표시 경계는 driver gate·기기에서 확인한다 |
| D44 | 드라이버 | 순수 기하 테스트가 `defer { window.close() }`로 만든 `NSWindow`를 XCTest autorelease-pool pop과 이중 해제해 SIGSEGV로 스위트를 중단시켰다 | 테스트 창은 닫지 않고 XCTest 주변 관례를 따른다. `isReleasedWhenClosed` 기본 true와 XCTest 안에서만 재현되는 pool-pop 충돌을 `CLAUDE.md`의 같은 AppKit 테스트 불변 원칙에 기록한다 | R7 gate exit 1; `testCenteringAndClampingConsumeTheSameVisibleFrame` 시작 뒤 xctest signal 11, `objc_release` → autorelease pool pop → `XCTMemoryChecker` 백트레이스; 같은 close/noclose 구성은 XCTest 밖에서 exit 0 | 수정 후 482번째 테스트가 스위트를 중단하지 않는지 driver gate에서 확인한다 |

## 전수 소탕 표

값의 흐름을 확인한 결과 `SetupWindowController.buildContent()`(488∼543)가 이 창의 유일한 `NSScrollView` 생성·documentView 배선 지점이며, `rebuildForLanguageChange()`(381∼395)는 384행에서 그 함수를 다시 호출해 새 scroll view를 `window.contentView`에 넣는다. ordinary `refresh()`와 각 알림 경로는 scroll view를 새로 만들지 않고 기존 stack의 값만 바꾼다. R3에서 `layout()` 안의 직접 창 변경은 `setContentSize`(104행)와 `moveInside`의 `setFrameOrigin`(167행)이며, 둘은 같은 deferred application 단위로 다룬다. R5에서 init은 생성 직후 한 번 읽은 launch visible rect로 placeholder를 `centerInside`하고, 첫 deferred application은 layout이 캡처한 같은 cycle의 visible rect로 `centerInside`와 `moveInside`를 함께 수행한다. 따라서 `buildContent()`의 자동 inset off와 deferred application은 최초 생성·ordinary refresh·언어 변경 재구성을 모두 덮지만, 언어 변경 restore는 application completion 뒤에 실행되어야 한다.

| 대상 | 판정 | 코드로 알 수 없는 이유 또는 `파일:행` |
|:--|:--|:--|
| 초기 `SetupWindowController` 생성: launch visible rect 선택 → placeholder center/move → `buildContent()`의 guide·install feedback·test result 기본 hidden → `updateTerminalControls()` → 첫 `refresh()` → `layoutSubtreeIfNeeded()` | deferred application 대상(항목 4) | `SetupWindowController.swift:191∼226`, `306∼345`, `406∼455`, `467∼745`; init이 `NSScreen.main`을 한 번 읽어 placeholder를 배치한 뒤 layout은 `window.screen`의 visible rect를 측정·저장하고, 실제 size 적용과 measured-size center는 다음 main-queue turn에서 실행된다. |
| `manifest` → `chromeCard.isHidden` | deferred application 대상(항목 4) | `SetupWindowController.swift:991∼1002`; manifest 완료/미완료가 card height를 바꾸고 기존 scroll view에서 `FittedContentStackView.layout()`이 최신 target을 예약한다. |
| `lastRequestAt`·extension state → `extensionCard.isHidden`, `utilityRow.isHidden` | deferred application 대상(항목 4) | `SetupWindowController.swift:1004∼1015`; socket evidence가 card를 줄이고 utility row를 되살리는 양방향이며 하나의 pending update가 최신 target을 적용한다. |
| `Settings.toolAvailability == nil` 또는 missing tools/wrapper advice → `toolsCard.isHidden`와 `toolsList` 재구성 | deferred application 대상(항목 4) | `SetupWindowController.swift:1117∼1141`; list removal/addition은 hidden 토글이 아니어도 intrinsic height를 바꾸며 다음 main turn의 size application을 공유한다. |
| `Settings.terminal`과 iTerm automation 상태 → `permissionSection.isHidden` | deferred application 대상(항목 4) | `SetupWindowController.swift:1029∼1050`; `select(terminal:)`의 iterm↔wezterm↔warp가 section을 늘리고 줄이는 양방향을 같은 pending/layout 경로로 처리한다. |
| `PermissionChecker.isAccessibilityGranted` → `accessibilitySection.isHidden` | deferred application 대상(항목 1·2·4) | `SetupWindowController.swift:1025`, `1051`; provider가 false→true/true→false를 만들고, 다음 main turn에 자동 inset off와 최신 window target을 함께 적용한다. |
| `select(terminal:)` → radio/note text → `refresh()` | deferred application 대상(항목 2·4) | `SetupWindowController.swift:949∼974`; terminal card text와 permission section이 동시에 변하고 `testWindowMatchesContentAcrossEveryTerminalTransition` 및 growth placement가 양방향을 본다. |
| `windowDidBecomeKey` → `refresh()`; System Settings에서 돌아온 Accessibility 상태의 실제 관측 지점 | deferred application 대상(항목 1·2·4) | `SetupWindowController.swift:235∼237`, `1348∼1354`; key 복귀가 상태 재조회 경계이며, refresh가 dirty한 stack의 최신 target을 pending update로 적용한다. |
| `.terminalCheckoutRequestHandled` → extension evidence/card, `.terminalCheckoutToolsChecked` → tools card | deferred application 대상(항목 4) | observer는 `SetupWindowController.swift:216∼222`, 발생원은 `Settings.swift:67∼71`, `96∼107`; 비동기 hidden 변화도 기존 scroll view의 자동 inset off와 pending layout application을 공유한다. |
| `languageChanged` → Settings notification·explicit `refresh()` → `rebuildForLanguageChange()` | deferred application·restore completion(항목 4) | `SetupWindowController.swift:303∼314`, `406∼455`; 새 scroll view가 size application을 예약하고, completion이 clip layout 뒤 305행의 capture place를 313행 restore 계약으로 복원한다. |
| `registerManifest`, `installInChrome`, `reshowInstall` → guide/feedback hidden 변경 및 `refresh()` | deferred application 대상(항목 4) | `SetupWindowController.swift:1235∼1264`, `1320∼1328`; 직접 label visibility를 바꾼 직후 refresh가 다시 상태를 덮고 다음 main turn에 intrinsic size를 적용한다. |
| `baseDirectoryEdited` → invalid status label text 또는 valid `refresh()` | deferred application 대상(항목 4) | `SetupWindowController.swift:1278∼1300`; invalid path는 refresh 없이 wrapping label만 바꾸므로 Fitted stack의 text intrinsic change가 pending size update를 통해 반영된다. |
| `requestPermission` completion → permission status·`refresh()` | deferred application 대상(항목 4) | `SetupWindowController.swift:1330∼1341`; iTerm callback은 Accessibility와 다른 비동기 경로지만 permissionSection의 크기 변화를 같은 deferred scroll setup으로 처리한다. |
| `requestAccessibility()` 즉시 `refresh()`와 key 복귀 refresh | deferred application 대상(항목 1·2·4) | `SetupWindowController.swift:1348∼1354`; prompt 직후 old state일 수 있고 key 복귀에서 실제 전이가 오며, 두 refresh 모두 최신 target 하나로 수렴한다. |
| `testTerminal` 시작/완료 → `testResultLabel.isHidden`·status text·`refresh()` | deferred application 대상(항목 4) | `SetupWindowController.swift:1360∼1382`; async 완료가 label visibility와 text를 바꾸고 권한 경로와 같은 intrinsic-size/deferred application oracle을 공유한다. |
| `windowWillClose` → guide/feedback/test result hidden | lifecycle guard(항목 4) | `SetupWindowController.swift:240∼247`; `windowWillClose`가 pending update와 completion을 버리고, 다음 `windowDidBecomeKey`가 같은 controller의 새 측정을 허용한다. |
| `visibleFrameOverride` 대입 → stack layout 예약과 visible rect 교체 | deferred application·placement setter 보존 | `SetupWindowController.swift:53∼55`, `77∼87`; `SetupWindowLayoutTests.swift:64∼74`, `180∼201`, `271∼296`; setter가 layout을 예약하고 visible rect가 바뀌면 target height가 같아도 다음 main turn에 최신 rect로 center/move를 적용한다. 순수 center/clamp 산술은 `testCenteringAndClampingConsumeTheSameVisibleFrame`에서 고정하고, 두 화면 선택은 기기에서 확인한다. |
| `FittedContentStackView.layout()` → `window.setContentSize`, `centerInside`, `moveInside`의 `window.setFrameOrigin` | 같은 deferred application 단위(항목 4·R5) | `SetupWindowController.swift:104∼183`; R3 하네스에서 패스 안 resize는 `win=702`·`clip=547`을 남겼고, `tile()`·`needsLayout`도 고치지 못했다. 이제 layout이 캡처한 하나의 visible rect를 다음 main turn의 size·center·clamp에 함께 사용한다. |
| init placeholder frame → launch screen의 `centerInside`·`moveInside` → 첫 실제 content size 적용 | launch rect·measured-size 이중 placement(항목 4·R5) | `SetupWindowController.swift:306∼345`, `104∼119`, `176∼188`; init은 `NSScreen.main`을 한 번 읽고 placeholder를 배치하며, deferred block은 `lastVisibleFrame`으로 실제 target을 center한 뒤 같은 rect로 clamp한다. |
| `visibleFrameOverride ?? window.screen?.visibleFrame` → clamp height와 placement | 한 cycle의 visible rect 정본(항목 3·R5) | `SetupWindowController.swift:75∼87`; production은 init의 launch seed 뒤 `window.screen`만 읽어 `NSScreen.main` 재선택을 피하고, 단위 테스트는 실제 화면 선택이 아닌 순수 center/clamp 산술과 기기 acceptance를 각각 맡는다. |
| `SetupWindowTestSupport.settle` → 두 설정 창 테스트와 notification post의 고정점 | 공용 run-loop 고정점·pending 완료 확인(항목 2·4) | `SetupWindowTestSupport.swift:44∼91`; 강제 `layoutSubtreeIfNeeded()`를 부르지 않고 main run loop만 펌프한다. `SetupWindowLayoutTests`와 `SetupWindowRedrawTests`의 fixture·post가 같은 helper를 사용하며, geometry가 안정되고 `windowUpdateScheduled`·`afterWindowUpdate`가 모두 비어 있을 때만 반환한다. `makeScrollingController()`는 적용된 window content height와 clip height를 출력·단언한다. |
| `8fccab5`의 visibility→settle→measure·clamp·scroll 계약 → 권한 text/hidden 전이 | 측정 계약 보존·적용 경계 보완(항목 3·4) | `git show 8fccab5`; 이번 입력은 `select(terminal:)`가 아니라 `windowDidBecomeKey` 뒤 `refresh()`이며, measurement는 layout 안에 남기고 window/clip 적용만 다음 main turn으로 이동한다. |
| `lastRequestedSize` early return과 `FittedContentStackView.layout()`의 measurement boundary | 최신 target 보존(항목 2·4) | `SetupWindowController.swift:64∼156`; fitting/target 측정과 early return은 유지하고, pending block은 캡처한 target이 아니라 실행 시점의 `lastRequestedSize`를 적용한다. |
| `capturePlace`/`restore`의 비뒤집힌 document top 및 hidden anchor fallback | restore completion 보존 필수(항목 3·4) | `SetupWindowController.swift:303∼385`; `SetupWindowRedrawTests.swift:224∼338`; 새 content view의 deferred size application과 clip layout 뒤에 anchor·focus·selection을 restore하며 비뒤집힌 `maxY` 계약은 바꾸지 않는다. |
| `AppDelegate.showSetupWindow()` → 창 표시·재개 | key lifecycle 경계 유지 | `AppDelegate.swift:16`, `30`, `35`, `111∼122`; launch·reopen·guidance callback 모두 `showWindow(nil)` 뒤 `makeKeyAndOrderFront(nil)`으로 수렴하므로 `windowDidBecomeKey`의 `resumeDeferredWindowUpdates()`가 현재 production 표시 경로를 덮는다. |

## 라운드 로그

라운드는 검증자의 전체 판정 사이의 구간이다. 리뷰(증분·최종·cold)마다 어느 커밋에 대한 것인지와 계측(승격 시각·리뷰 시작·종료·왕복 수)을 적고, 리뷰 하나는 차단·수정·실측·판정 네 줄이다. 보고서 원문은 스크래치패드 파일 경로로 가리킨다 — 옮겨 적지 않는다. R0은 설계 리뷰다 — 차단 자리에 반박, 수정 자리에 처리(반영/기각 + 원장 번호)를 적고 둘 다 드라이버가 지정한다.

### R0

#### 설계 리뷰 — 계획 초안 · 리뷰 드라이버 직접 · 왕복 1 · 원문 `/private/tmp/claude-501/-Users-choongjaelee-Codes-terminal-checkout/4ffc53dd-bc2a-45a7-9448-a47a2106d5f6/scratchpad/R0-review1.md`

- 반박: ① acceptance oracle이 높이만 봐서 보고된 배치 증상을 재현하지 못한다 ② `layout()` 안의 `setContentSize` 재진입에 대한 수렴 oracle이 없고 `settle`이 `layoutSubtreeIfNeeded()`를 한 번만 부른다 ③ 화면 선택 분기가 seam 밖이라 어떤 테스트도 지나지 않는다 ④ 선행 수정 `8fccab5`가 왜 못 막았는지를 규명하는 항목이 없다 ⑤ 재현 red 전에 수정 항목이 배정되는 순서다 ⑥ 완료의 정의에 사용자가 본 화면(기기 절차)이 고정돼 있지 않다 ⑦ 배치 점검 표가 clone 기준이라 오해를 만들고 커밋 링크 해시가 39자로 깨졌다 ⑧ `refresh()` 수치가 세는 명령 없이 적혀 실측(23)과 다르다
- 처리: 전부 반영 — 항목을 재현(1∼3) → 수정(4) → 기기(5) 순서로 재편, 배치·수렴 oracle 추가, 화면 분기와 `8fccab5` 대조를 항목 3으로 신설, 기기 절차 2건을 완료의 정의에 고정 (D6∼D13). 기각 없음
- 실측: 드라이버 baseline `swift test --package-path app` → 481 tests / 1 skipped / 0 failures / exit 0, `node --test` → 216 pass / 0 fail (기준 트리 `a13fa48`). 반박 1의 기하학은 `clipgeom.swift` 실측으로 확정 (D16). 구현자 샌드박스에서는 게이트가 중첩 `sandbox-exec`로 돌지 않는다 — 앞으로 게이트는 드라이버가 clone에서 돌린다 (D5)
- 판정: "이 계획으로 시작하는 데 합의한다. 단 R1 첫 배정은 항목 1∼3의 재현·계측뿐이고, 항목 4의 수정 배정은 red를 본 뒤에만 나간다."

### R1

#### 리뷰 1 — 증분 · R1 재현 커밋 · 리뷰 드라이버 직접 · 왕복 2 · 원문 `/private/tmp/claude-501/-Users-choongjaelee-Codes-terminal-checkout/4ffc53dd-bc2a-45a7-9448-a47a2106d5f6/scratchpad/R1-fix1.md`

- 차단: 1차 제출이 컴파일되지 않았다 — `String(CGFloat)` 이니셜라이저 없음, `firstDescendant(role:)`가 다른 테스트 파일의 `fileprivate`. 그리고 `FittedContentLayoutPass`가 `FittedContentStackView`의 설계 주석과 클래스 선언 사이에 들어가 `8fccab5`의 근거 원문이 엉뚱한 타입의 문서가 됐다
- 수정: 컴파일 에러 2건, 주석 재부착 원복, `visible.minY` 단언에 "콘텐츠가 뷰포트에 들어올 때만" 전제 게이트, production 가시성 확대 원복 (D22)
- 실측: 전체 게이트 exit 1 — 481 tests / 1 skipped / 2 failures, 둘 다 신규 케이스. 빈 띠 32.0pt, 드라이버 독립 복제 `fittedloop.swift`의 32.0과 일치
- 판정: 재현 확인. D4(a)(b)·D7 크기 축 기각, 결함 축은 clip bounds origin (D19·D20). 크기 차이는 미해명으로 남긴다 (D21)

### R2

#### 리뷰 2 — 증분 · R2 수정 · 리뷰 드라이버 직접 · 왕복 3 · 원문 `/private/tmp/claude-501/-Users-choongjaelee-Codes-terminal-checkout/4ffc53dd-bc2a-45a7-9448-a47a2106d5f6/scratchpad/R2-impl.md`

- 차단: 구현자의 메커니즘 규명이 `automaticallyAdjustsContentInsets`를 놓쳤고, 제안한 (c) origin clamp는 원인을 모른 채 결과를 보정하는 패치였다. (b) 기각 근거로 쓴 "resize 경계에서 constrainBoundsRect가 호출된다는 증거가 없다"는 미검증 주장이었다
- 수정: `buildContent()`에서 자동 content inset을 끈다(원인 제거). 축소·확대 양방향과 터미널 전이·긴 문서 정지 상태에 배치 단언을 추가. 동어반복 단언 1건 제거 (D30)
- 실측: 게이트 481 / 1 skipped / 0 failures / exit 0. 토글 역적용 시 481 / 1 skipped / 11 failures / exit 1. 구성 대조 — auto insets ON: clip.origin.y 32/32/0, OFF: 0/0/0. flipped 여부는 무관(B ≡ D)
- 판정: 항목 4 구현이 측정된 원인 하나를 닫았다. 실기기 154pt가 이것으로 닫히는지는 미확인 — 기기 검증(항목 5)이 남았고, 남으면 별건 원인으로 새 항목을 세운다

## 열린 질문

- 항목 1의 provider seam을 `PermissionChecker`의 closure로 둘지 별도 상태 함수로 둘지, 실제 TCC 호출을 production default로 보존하는 최종 이름은 무엇인가.
- 항목 1 seam을 연결한 뒤 현재 tree에서 false→true test가 실제로 red가 되는가. red가 되지 않으면 `FittedContentStackView.layout()`의 pass 경계를 관찰할 수 있는 최소한의 test-only instrumentation seam이 추가로 필요하다.
- R2에서 R1의 32pt는 중복 `automaticallyAdjustsContentInsets`의 32pt로 특성화됐고(D27), 실기기 154pt가 이 변경으로 닫히는지는 아직 미해명이다. 남으면 clamp를 추측해 추가하지 않고 별건 원인으로 새 항목을 세운다.
- R3의 redraw 3건은 이전 강제-layout fixture 때문에 실패했으나(D36), 공용 settle이 `pendingWindowCompletion=true`를 기다린 뒤 field-editor selection까지 green으로 만들었다. 따라서 해당 실패는 제품 회귀가 아닌 하네스 문제로 닫혔다(D39).
- R5의 두 화면 선택 불일치는 `NSScreen.main`과 `window.screen`을 단위 테스트에서 서로 다른 실제 화면으로 만들 수 없으므로 (i) red로 닫지 않는다. 순수 `centerInside`·`moveInside` 계약은 (ii)로 고정하고, 수정 전 최종 `X=0` 및 세로 화면 중앙 `X=420`의 차이는 기기 acceptance에서 재확인한다.
- 권한 provider 전이와 실제 System Settings 복귀가 동일한 `windowDidBecomeKey` 경계를 통과하는지, 재설치 후 cdhash reset이 완료되기 전의 중간 상태가 별도 수동 시나리오를 요구하는지는 기기 acceptance에서 확인해야 한다.
