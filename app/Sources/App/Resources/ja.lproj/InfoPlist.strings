/* Info.plist keys that face the user. Deliberately empty: the only such key is
   NSAppleEventsUsageDescription, whose en and ko values item 11 wrote and whose translation into
   this language is item 24's; a placeholder in the wrong language would be a claim this file has
   no business making. macOS falls back to the value in `app/Info.plist` while this is empty. The
   file exists so the five catalogs have the same shape and item 7 has something to compare byte
   for byte. */
